# Role
Expert Vue 3 & Frontend Engineer.

# Task
Fix the "Failed to resolve component" warnings for 'XCircle' and 'Scan' in the `Classifier.vue` file.

# Context
- Framework: Vue 3 (Composition API with <script setup>)
- Icon Library: lucide-vue-next (already installed via npm)
- Error: [Vue warn]: Failed to resolve component: XCircle / Scan

# Requirement
1. Analyze the provided `Classifier.vue` code.
2. Add the missing named imports from 'lucide-vue-next' into the `<script setup>` block.
3. Ensure the components are correctly registered for use in the `<template>`.
4. Strictly maintain the existing image classification logic and FastAPI integration.

# File Content to Fix:
[PASTE YOUR ENTIRE Classifier.vue CODE HERE]